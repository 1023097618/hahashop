<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.dao.AuthDao">
    <!--通过登录时输入的用户名查询用户信息-->
    <select id="login" resultType="com.mall.entity.User">
        SELECT id, username, password ,privilege ,userPhone ,userAddress, userRealName FROM User
        WHERE username = #{username}
    </select>

    <!--更改密码-->
    <update id="renewPassword">
        UPDATE User
        SET password = #{password}
        WHERE username = #{username}
    </update>

    <!--获取用户信息-->
    <select id="getInfo" resultType="com.mall.entity.User">
        SELECT id, username, privilege ,userPhone ,userAddress, userRealName FROM User
        WHERE id = #{userid}
    </select>

    <!--selectUsernameByExample-->
    <select id="selectUsernameByUsername">
        SELECT username FROM User
        WHERE username = #{username}
    </select>

    <!--register-->
    <insert id="register">
        INSERT INTO User(username, password, privilege, userPhone, userAddress, userRealName)
        VALUES (#{username}, #{password}, #{priviledge}, #{userPhone}, #{userAddress}, #{userRealName})
    </insert>

</mapper>